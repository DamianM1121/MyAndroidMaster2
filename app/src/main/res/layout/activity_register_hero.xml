<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="#99000000"
    tools:context=".superheroapp.Database.SuperHeroDB.RegisterView.RegisterHeroActivity">
<!--RecyclerView para las vistas de registro    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRegisterViewHeros"
        android:layout_width="0dp"
        android:layout_height="0dp"
        tools:listitem="@layout/register_view_data"
        app:layout_constraintBottom_toTopOf="@+id/btnClearRegister"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnClearRegister"
        android:layout_width="150dp"
        android:layout_height="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/rvRegisterViewHeros"
        tools:text="clear all" />
</androidx.constraintlayout.widget.ConstraintLayout>

